use crate::math::Vec2;

pub struct AABB{
    s:Vec2,
    e:Vec2,
}

impl AABB {
    pub fn new(start:Vec2,end:Vec2) -> Self{
        let (start,end):(Vec2,Vec2) = {
            if start.x < end.x && start.y < end.y{
                
            }


            todo!()
        };
        
        todo!()
    }
    
    fn get_left(){
        
    }
}